{
  "name": "@colder/shared-types",
  "version": "0.0.1",
  "description": "Shared TypeScript types for Colder",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}